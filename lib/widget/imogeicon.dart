import 'package:flutter/material.dart';

class ImogeIcon extends StatelessWidget {
  const ImogeIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text(""));
  }
}
